[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
ansible-cmdb = ">=1.31"
ansible-core = ">=2.17.1"
ansible-lint = {extras = ["yamllint"], version = ">=6.18.0"}
cryptography = ">=42.0.4"
zabbix-api = ">=0.5.4"
netaddr = ">=0.8.0"
secenv = ">=2.1.2"

[dev-packages]
pre-commit = ">=3.7.1"
pre-commit-hooks = ">=4.4.0"

[requires]
python_version = "3.11"
