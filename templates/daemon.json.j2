{
    "dns": [
        "{{ docker_opts_dns_1 }}",
        "{{ docker_opts_dns_2 }}"
    ],
    "dns-search" : ["{{ docker_opts_dns_search }}"],
    "live-restore": true,
    "experimental": true,
    "log-driver": "json-file",
    "log-opts": {
       "max-size": "100m",
       "max-file": "1"
     }
}
